type Course = {
    courseSubjectId: string;    // looks like "COMPSCI"
    courseNumber: string;       // looks like "187"
    courseTitle: string;
    semester: string;
    credits: number;
    professor: string;
    grade: string;
    notes: string;
    courseDescription: string;
}
